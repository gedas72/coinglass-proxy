# CoinGlass Proxy (Vercel)

Proxy, skirtas gauti CoinGlass Open Interest duomenis per Vercel, apeinant IP blokavimą.

## Endpoint:
GET /api/coinglass?symbol=BTCUSDT

Raktas saugomas aplinkoje: `COINGLASS_API_KEY`.
